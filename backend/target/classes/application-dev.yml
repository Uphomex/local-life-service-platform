server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/local_life_db?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true
    username: admin
    password: password

  redis:
    host: localhost
    port: 6379
    password: password

logging:
  level:
    com.example.life: DEBUG
    org.springframework.security: DEBUG
